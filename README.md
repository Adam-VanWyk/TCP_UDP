# TCP_UDP

To run this program, simply type 'python main.py' and the terminal output will direct you in graph creation. Only enter integer numbers and to make connections separate node indices with spaces. Click on nodes to send messages between them. 
